# 🎬 Zero-Cost Video Pipeline | Requirements (v0.3)
# Минимальный рабочий набор для этапа Dry-Run и веб-интерфейса.
# Остальные пакеты (openvino, diffusers, faiss, moviepy, etc.) будут добавлены позже.

# === ИНТЕРФЕЙС ===
gradio==4.26.0          # Веб-интерфейс для запуска и тестов

# === СИСТЕМНЫЕ УТИЛИТЫ ===
python-dotenv==1.0.1    # Чтение настроек из .env
psutil==5.9.8           # Мониторинг RAM/CPU (для менеджера моделей)
tqdm==4.66.2            # Прогресс-бары в консоли

# === ОБРАБОТКА ДАННЫХ ===
numpy==1.26.4           # Быстрые операции с массивами (маски, кадры)
Pillow==10.3.0          # Работа с изображениями (keyframe, превью, логотипы)
