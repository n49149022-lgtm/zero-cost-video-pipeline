# 🎬 Zero-Cost Video Pipeline | Requirements
# Этот файл — «список покупок» для Python.
# Он скажет системе, какие библиотеки нужны для работы пайплайна.
# Строки с # — комментарии. Они не устанавливаются, только объясняют.
# Пока — план. Не запускайте установку, пока не будет готова логика.

# === ЯДРО ГЕНЕРАЦИИ (CPU-оптимизированные) ===
# diffusers==0.27.2        # Управление моделями генерации видео/изображений
# transformers==4.39.3     # Нейросетевые модели (текст, эмбеддинги)
# accelerate==0.28.0       # Ускорение вычислений на обычных процессорах
# optimum[openvino]        # Оптимизация нейросетей под Intel/AMD CPU

# === ОБРАБОТКА ВИДЕО И КАДРОВ ===
# opencv-python-headless==4.9.0.80  # Работа с пикселями, масками, склейкой
# moviepy==1.0.3                    # Сборка итогового .mp4, звук, субтитры
# Pillow==10.3.0                    # Открытие/сохранение изображений
# numpy==1.26.4                     # Быстрые математические операции с данными

# === КЭШ И СЕМАНТИЧЕСКИЙ ПОИСК ===
# faiss-cpu==1.7.4                  # Мгновенный поиск похожих промптов/кадров
# sentence-transformers==2.7.0      # Превращение текста в векторы (смыслы)

# === УТИЛИТЫ ===
# tqdm==4.66.2             # Красивые прогресс-бары в консоли
# python-dotenv==1.0.1     # Чтение настроек из файла .env
